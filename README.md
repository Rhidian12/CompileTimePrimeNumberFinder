# CompileTimePrimeNumberFinder
A simple templated program to check a series of numbers (starting at 0) until N, checking which of those are prime numbers.

# Requirements:
> C++17<br>
> iostream

# Usage:
